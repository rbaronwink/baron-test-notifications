'use strict'

import { expect } from 'chai'
import * as sinon from 'sinon'

import * as serviceController from '../src/api/controller'

describe('Controller', () => {
  it('should say true', function () {
    expect(true).to.be.true
  });
})